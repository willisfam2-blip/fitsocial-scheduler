'use client';
import {useEffect,useState} from 'react';
const KEY='fitsocial-posts-v1';
export default function Home(){const [posts,setPosts]=useState([]);const [tab,setTab]=useState('create');const [caption,setCaption]=useState('');const [date,setDate]=useState('');const [time,setTime]=useState('19:00');const [media,setMedia]=useState(null);useEffect(()=>{try{setPosts(JSON.parse(localStorage.getItem(KEY)||'[]'))}catch{}},[]);function save(next){setPosts(next);localStorage.setItem(KEY,JSON.stringify(next))}function add(status){if(!media)return alert('Add a photo or video first.');const p={id:Date.now(),caption,date,time,status,name:media.name,type:media.type,created:new Date().toISOString()};save([p,...posts]);setCaption('');setMedia(null);alert(status==='scheduled'?'Post scheduled. Connect Meta to enable automatic publishing.':'Draft saved.')}return <main><header><div><b>FitSocial</b><span> Scheduler</span></div><nav>{['create','planner','media','settings'].map(x=><button className={tab===x?'active':''} onClick={()=>setTab(x)} key={x}>{x[0].toUpperCase()+x.slice(1)}</button>)}</nav><div className="account">Instagram · @MacroSnap2026</div></header><section className="wrap">{tab==='create'&&<><h1>Create Instagram Post</h1><p className="sub">Upload your content, write a caption and publish now or schedule it.</p><div className="card"><label>Media</label><label className="upload"><input type="file" accept="image/jpeg,image/png,image/webp,video/mp4,video/quicktime" onChange={e=>setMedia(e.target.files?.[0]||null)}/><strong>+ Add photo or video</strong><small> JPG, PNG, WebP, MP4 or MOV · up to 500 MB</small>{media&&<em>{media.name}</em>}</label><label>Caption</label><textarea value={caption} onChange={e=>setCaption(e.target.value)} placeholder="Write your caption and hashtags…"/><div className="grid"><div><label>Publish date</label><input type="date" value={date} onChange={e=>setDate(e.target.value)}/></div><div><label>Time</label><input type="time" value={time} onChange={e=>setTime(e.target.value)}/></div></div><p className="tz">Times are saved in America/Edmonton.</p><div className="actions"><button className="ghost" onClick={()=>add('draft')}>Save Draft</button><button className="secondary" onClick={()=>add('scheduled')}>Schedule Post</button><button className="primary" onClick={()=>alert('Meta connection is required before Instagram can publish. Open Settings next.')}>Publish Now</button></div></div></>}{tab==='planner'&&<><h1>Planner</h1><div className="card"><h2>Scheduled & drafts</h2>{posts.length===0?<p>No posts yet.</p>:posts.map(p=><article key={p.id}><div><b>{p.name}</b><p>{p.caption||'No caption'}</p></div><span className={'badge '+p.status}>{p.status}</span><time>{p.date||'No date'} {p.time}</time></article>)}</div></>}{tab==='media'&&<><h1>Media Library</h1><div className="card"><p>Media selected for posts will appear here once cloud storage is connected.</p></div></>}{tab==='settings'&&<><h1>Settings</h1><div className="card"><h2>Instagram connection</h2><p>Account: <b>@MacroSnap2026</b></p><p className="notice">Meta publishing is not connected yet. The next setup step is adding your Meta App credentials securely in Vercel environment variables.</p><button className="primary" onClick={()=>alert('Next: deploy to Vercel, then configure Meta credentials.')}>Connect Instagram</button></div></>}</section></main>}